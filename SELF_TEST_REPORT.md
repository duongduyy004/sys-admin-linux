# Self-Test Report

Status: generated with the project.

Run this command to verify the current checkout:

```bash
bash tests/self_test.sh && echo "READY FOR HANDOVER"
```

The report is considered current only after `tests/self_test.sh` exits successfully.
